<h1>Editing perform</h1>
    <% @perform.spectacle.photos.each do | photo | %>
      <%= cl_image_tag photo.key, height: 150, width: 200, crop: :fill %>
      <p><strong>photo key :</strong> <%= photo.key %></p>
    <% end %>
<%#= cl_image_tag("#{@perform.spectacle.photo}", height: 90, width: 160, :crop=>"limit") %></strong></p>
<p><strong>
<%= @perform.spectacle.title %>
</strong></p>
<div class="card p-4">
  <%= render "form", perform: @perform %>
</div>

<br>

<div>
  <%= link_to "Show this perform", @perform %> |
  <%= link_to "Back to performs", performs_path %> |
</div>
